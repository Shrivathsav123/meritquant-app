node_modules
dist
.env*.local

# design-sync: staged converter scripts, build output, and machine state —
# see .design-sync/config.json / NOTES.md for the durable, committed set
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules
